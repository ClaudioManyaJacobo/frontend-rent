# Frontend — BackNest Admin

Angular 19 + integración con **back-nest**.

## Desarrollo

```bash
npm install
npm start
```

API vía proxy: `/api` → `http://localhost:3000` (requiere backend en ejecución).

## Arquitectura

Ver [ARCHITECTURE.md](./ARCHITECTURE.md).
